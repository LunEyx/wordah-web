import { IconButton } from '@chakra-ui/react'
import { QuestionOutlineIcon } from '@chakra-ui/icons'

const ChallengeButton = () => {
  return <IconButton aria-label="menu" icon={<QuestionOutlineIcon />} />
}

export default ChallengeButton
